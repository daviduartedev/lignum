/**
 * Seed de massa legado Movix — desativado após teardown Stage 8.
 * Use o seed mínimo: npm run db:seed
 */
console.log("[seedBulk] Desativado — domínio Movix removido no cycle 0720.");
process.exit(0);
