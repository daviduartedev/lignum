# 10 — OS / detalhe da ordem de produção (sem tela do Stitch)

O Stitch **não gerou** esta tela. Para implementar (cycle 0720), use como base:

- **`09-producao-kanban/`** — o board de produção do Stitch; o detalhe abre a partir de um card.
- Padrão de tabs + cards do `STITCH-DESIGN.md`.

Conteúdo esperado: cabeçalho com status, tabs (ficha técnica/BOM, funcionários vinculados, fotos
do progresso, histórico), indicador de baixa automática de estoque. Adaptar de `ServiceOrder`
(Movix) → `ProductionOrder`.
