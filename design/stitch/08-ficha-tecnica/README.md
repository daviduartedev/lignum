# 08 — Ficha técnica / BOM (sem tela do Stitch)

O Stitch **não gerou** esta tela. Para implementar (cycle 0713), use como base:

- **`07-orcamento-novo/`** — a ficha técnica é derivada do orçamento aprovado; reusar o layout
  de itens/medidas/materiais.
- Padrão de tabela do `STITCH-DESIGN.md` para a lista de materiais (BOM).

Conteúdo esperado: medidas da carroceria, lista de materiais (BOM) com quantidades, serviços,
e botão para gerar PDF (pipeline `@react-pdf/renderer` reusado do Movix).
