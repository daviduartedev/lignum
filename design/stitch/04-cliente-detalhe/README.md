# 04 — Cliente / detalhe (sem tela do Stitch)

O Stitch **não gerou** esta tela. Para implementar (cycle 0706), use como base:

- **`03-clientes/`** — a lista de clientes do Stitch (padrão de cabeçalho, busca, ações).
- Padrão de cards/seções do `STITCH-DESIGN.md`.

Conteúdo esperado: dados cadastrais (PF/PJ), documentos anexados, botão "Consultar CPF/CNPJ"
com autofill, histórico de orçamentos/compras. Reusar `Client`/`ClientDocument` do Movix.
